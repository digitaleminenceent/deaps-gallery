/**
 * DEAPS Core Constants
 */

(() => {
    "use strict";

    // Cipta objek DEAPS jika belum ada
    window.DEAPS = window.DEAPS || {};

    // Semua nilai tetap disimpan di sini
    window.DEAPS.Constants = {
        APP: {
            NAME: "DEAPS Gallery",
            VERSION: "1.0.0"
        }
    };

})();